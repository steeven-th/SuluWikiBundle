<style>
	/*** Custom styles for the Sulu Wiki Bundle ***/

	/* Keyboard key styling */
	kbd {
		display: inline-block;
		padding: 3px 6px;
		margin: 0 2px;
		font-size: 0.9em;
		line-height: 1.4;
		color: #333;
		background-color: #f7f7f7;
		border: 1px solid #ccc;
		border-radius: 4px;
		box-shadow: inset 0 -2px 0 #ccc;
		font-family: 'Courier New', Courier, monospace;
		text-transform: uppercase;
	}

	kbd:before,
	kbd:after {
		content: "";
		display: inline-block;
		vertical-align: middle;
		height: 6px;
		width: 2px;
		background-color: #ccc;
		border-radius: 1px;
		margin: 0 2px;
	}

	/* Headings */
	h1 {
		font-size: 2.5rem;
	}

	h2 {
		font-size: 2rem;
	}

	h3 {
		font-size: 1.75rem;
	}

	h4 {
		font-size: 1.5rem;
	}

	h5 {
		font-size: 1.25rem;
	}

	h6 {
		font-size: 1rem;
	}

	/* Code blocks */
	pre code {
		border-radius: 8px;
		white-space: pre;
	}

	/* Blockquotes */
	.quote {
		display: flex;
		align-items: center;
		padding: 10px;
		margin: 10px 0;
		border-radius: 8px;
		border-left: 8px solid;
		background-color: #f0f0f0;
		font-style: italic;
	}

	.quote-icon {
		flex-shrink: 0;
		margin-right: 10px;
		border-radius: 50%;
		color: white;
		width: 40px;
		height: 40px;
		display: flex;
		justify-content: center;
		align-items: center;
	}

	.quote.standard {
		background-color: #e0e0e0;
		border-color: #b0b0b0;
	}

	.quote.standard .quote-icon {
		background-color: #b0b0b0;
	}

	.quote.info {
		background-color: #e0f7ff;
		border-color: #00b0ff;
	}

	.quote.info .quote-icon {
		background-color: #00b0ff;
	}

	.quote.warning {
		background-color: #fff7e0;
		border-color: #ffb800;
	}

	.quote.warning .quote-icon {
		background-color: #ffb800;
	}

	.quote.danger {
		background-color: #ffe0e0;
		border-color: #ff4d4d;
	}

	.quote.danger .quote-icon {
		background-color: #ff4d4d;
	}

	.quote.success {
		background-color: #e0ffe0;
		border-color: #28a745;
	}

	.quote.success .quote-icon {
		background-color: #28a745;
	}

	/* Styles for the separator line */
	.separator {
		border: none; /* Remove the default border */
		height: 1px;  /* Set the height of the line */
		background-color: #ccc; /* Set the color of the line */
		margin: 20px 0; /* Add vertical spacing around the line */
		position: relative; /* Position relative for pseudo-elements */
	}

	.hljs-copy-button {
		top: 2rem;
		cursor: pointer;
	}
</style>
